# Shared with reasoning; install whichever you need for hypergraph stages.
openai>=1.40.0
httpx>=0.27.0
tenacity>=8.2.0
python-dotenv>=1.0.0
tiktoken>=0.7.0
numpy>=1.24.0
asyncpg>=0.29.0
ijson>=3.2.0
sentence-transformers>=3.0.0
torch>=2.2.0
timm>=0.9.0
ollama>=0.3.0

# Stage 0 (MinerU PDF parse) — install separately if needed:
#   pip install -U "mineru[core]"
